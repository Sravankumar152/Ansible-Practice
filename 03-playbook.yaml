- name: Installing Nginx
  hosts: localhost
  connection: local
  become: yes

  tasks:
  - name: Install Nginx
    ansible.builtin.package:
      name: nginx
      state: present
      use: dnf

  - name: Running the Nginx
    ansible.builtin.service:
      name: nginx
      state: started
