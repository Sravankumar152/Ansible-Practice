- name: Installing Nginx
  hosts: Node
  tasks:
  - name: Install Nginx
    become: yes
    ansible.builtin.package:
      name: nginx
      state: present
      use: dnf

  - name: Running the Nginx
    become: yes
    ansible.builtin.service:
      name: nginx
      service: started